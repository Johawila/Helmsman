# --- .NET ---
bin/
obj/
*.user
[Dd]ebug/
[Rr]elease/
artifacts/

# --- Node / Vite ---
node_modules/
dist/
.vite/
*.local

# --- Editors / OS ---
.vs/
.idea/
.DS_Store

# --- Secrets / cluster credentials (never commit) ---
# Helmsman reads the user's own ~/.kube/config at runtime; no kube/cert/secret
# material should ever live in this repo.
*.env
.env.*
kubeconfig
kubeconfig.*
*.kubeconfig
.kube/
*.pem
*.key
*.crt
*.pfx
appsettings.*.Local.json
secrets.json
